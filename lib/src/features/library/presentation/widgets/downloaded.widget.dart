import 'package:flutter/material.dart';

class Downloadedwidget extends StatelessWidget {
  const Downloadedwidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}